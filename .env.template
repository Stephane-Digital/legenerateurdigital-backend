# ==========================================================
# 🌍 LE GÉNÉRATEUR DIGITAL — BACKEND (Template d’environnement)
# ==========================================================
# ⚙️ Ce fichier est un modèle de configuration (.env)
#    À copier sous le nom .env pour usage local ou Render.
# ==========================================================

# ----------------------------------------------------------
# 🧱 APPLICATION
# ----------------------------------------------------------
APP_ENV=production
APP_NAME="Le Générateur Digital API"
APP_VERSION=1.0.0
DEBUG=True
LOG_LEVEL=info

# ----------------------------------------------------------
# 🗄️ BASE DE DONNÉES (Render PostgreSQL ou locale)
# ----------------------------------------------------------
# Exemple Render :
# DATABASE_URL=postgresql://legenerateurdigital_db_user:password@hostname:5432/legenerateurdigital_db?sslmode=require
# Exemple local :
# DATABASE_URL=sqlite:///./prisma/dev.db
DATABASE_URL=postgresql://legenerateurdigital_db_user:password@hostname:5432/legenerateurdigital_db?sslmode=require

# ----------------------------------------------------------
# 🔐 SÉCURITÉ & JWT
# ----------------------------------------------------------
JWT_SECRET=à_remplacer_par_une_chaîne_unique_sécurisée
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ----------------------------------------------------------
# 🌐 CORS (Origines autorisées)
# ----------------------------------------------------------
CORS_ORIGINS=https://le-generateur-digital.vercel.app,http://localhost:3000

# ----------------------------------------------------------
# 🤖 INTELLIGENCE ARTIFICIELLE (IA LGD)
# ----------------------------------------------------------
# Utilisé par les modules Automatisations & Génération de contenu
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_PROJECT_ID=proj_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini

# ----------------------------------------------------------
# 💌 EMAIL / SMTP (futur module “Campagnes”)
# ----------------------------------------------------------
MAIL_FROM="contact@legenerateurdigital.com"
MAIL_FROM_NAME="Le Générateur Digital"
MAIL_USERNAME=contact@legenerateurdigital.com
MAIL_PASSWORD=mot_de_passe_smtp
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587

# ----------------------------------------------------------
# 📦 STOCKAGE & CHEMINS
# ----------------------------------------------------------
UPLOAD_DIR=uploads
STATIC_DIR=static
LOGS_DIR=logs

# ----------------------------------------------------------
# 🌍 URLS GLOBALES
# ----------------------------------------------------------
BACKEND_URL=https://legenerateurdigital-backend-m9b5.onrender.com
FRONTEND_URL=https://le-generateur-digital.vercel.app

# ----------------------------------------------------------
# 🧩 DÉVELOPPEMENT LOCAL
# ----------------------------------------------------------
RELOAD=True
TEST_MODE=False
PORT=8000
HOST=127.0.0.1

# ==========================================================
# ✅ FIN DU TEMPLATE .ENV
# ==========================================================
